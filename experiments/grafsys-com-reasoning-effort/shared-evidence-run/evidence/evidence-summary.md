# Evidence Summary

Shared evidence was captured from `https://grafsys.com/` using safe Playwright MCP calls only. `browser_run_code_unsafe` was not used.

Captured pages:

- Homepage: `https://grafsys.com/`

Observed structure:

- A skip link to `#content`.
- Main article with heading `Graf Systems`.
- Logo image with accessible name `Graf Systems`.
- Text: `Solutions & Design by Knut Graf`.
- Footer with a same-site `Graf Systems` home link.
- Footer with an external WordPress attribution link.

Responsive evidence:

- Desktop and mobile snapshots expose the same accessibility structure.
- Mobile viewport was set to 390x844 before the mobile snapshot.

Boundaries:

- No additional same-site navigation pages were visible in the accessibility tree.
- Do not infer backend systems, data entities, forms, or API contracts from this evidence.
- Workers may model only the observed homepage, components, visible links, and basic view states.

